import { SiteHeader } from "../site-header";

export const App = () => {
    return (
        <div className="wrapper">
            <SiteHeader />
            <main className="page"></main>
        </div>
    );
};
